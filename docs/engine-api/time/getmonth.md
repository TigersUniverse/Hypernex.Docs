# GetMonth

Returns the month value for the Time

The following example prints the current month in a year.

**JavaScript**
```js
print("The current month is " + new Time().GetMonth())
```

**Lua**
```lua
print("The current month is "..tostring(Time().GetMonth()))
```